# One file Sync_folder apllication for sync two folders.

## Run procedure 
./ main.py 'path to source folder' 'path to replica folder' <interval between synchronizations> <amount of synchronizations> 'path to log file'   
